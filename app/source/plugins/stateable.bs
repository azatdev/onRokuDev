class Stateable
    function initPlugin(options = {})
        ? "Init Safeable " m.top.subType()
    end function

    function getReferences()
        return {
            initPlugin: m.initPlugin
        }
    end function
end class
