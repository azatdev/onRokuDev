const mixins = require("../mixins");
const palette = mixins["Palette"]

module.exports = {
    "Metadata": {
        "Div": {
            "Quantico": {
                size: 24
            }
        }
    }
};
