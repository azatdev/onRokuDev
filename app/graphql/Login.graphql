mutation Login($email: String!, $password: String!) {
  login(input: {
    identifier: $email
    password: $password
    provider: "local"
  }) {
    jwt
    user {
      id
      documentId
      username
      email
    }
  }
}
