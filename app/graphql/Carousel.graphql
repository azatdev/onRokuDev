query Carousel($documentId: ID!) {
  carousel(documentId: $documentId) {
    contents {
        complimentaryColor
        documentId
        title
        type
        images {
          imageType
          data {
            url
          }
        }
    }
  }
}
