query Carousel($documentId: ID!, $pagination: PaginationArg) {
  carousel(documentId: $documentId) {
    contents(pagination: $pagination) {
        complimentaryColor
        documentId
        title
        type
        images {
          imageType
          data {
            url
          }
        }
    }
  }
}