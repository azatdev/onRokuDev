import "pkg:/components/_ord/init.bs"

sub init()
    exec(sub(_options = {})
        m.list = top().findNode("tabList")
        safeObserve("data", onDataChange)
    end sub)
end sub


sub onDataChange(options = {})
    data = options.data
    m.model = data.content

    liveConfig({ field: "config", keyPath: "TabsCarousel" }, m.model)

    m.list.data = {
        content: m.model
    }
end sub
