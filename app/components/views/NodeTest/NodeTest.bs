import "pkg:/components/~boilerplate/azat.bs"


sub onInit()
    subscribe({
        "TEST_EVENT": "onTestEvent"
    })
end sub


sub onTestEvent(data, msgInfo)
    ?"onTestEventasdfasdf " data msgInfo.timespan.totalMilliseconds()

    ' exec("testFunction", { demand: "MOAR PIZZA" }, app())
end sub
