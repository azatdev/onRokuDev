const iconConfig = require("../layout/icons");
const Icons = iconConfig["Icons"]

module.exports = {
    "ControlItems": {
        "hideOverlay": {
            id: "hideOverlay",
            icons: [Icons.arrowLeft],
            opacity: 0.4,
            title: "TKEY_HIDE_OVERLAY"
        }
    }
}